# Lishanth — Portfolio

An IDE-themed personal portfolio built with React + Vite.

## Run locally
npm install
npm run dev

## Build for production
npm run build

## Deploy
Push this repo to GitHub, then import it on vercel.com (zero config needed — Vite is auto-detected).
